INCLUDE_DIRECTORIES( ${PROJECT_SOURCE_DIR}/src )

ADD_EXECUTABLE(unit_prod src/unit_prod.cpp)

TARGET_LINK_LIBRARIES(unit_prod ${OPENTISSUE_LIBS} ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY} )
INSTALL(
  TARGETS unit_prod
  RUNTIME DESTINATION  bin/units
  )

ADD_TEST( unit_prod unit_prod )

